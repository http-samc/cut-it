from os import stat
from resource import PATH

class store:

    @staticmethod
    def add_login(login):
        #adds login to data
        pass
    
    @staticmethod
    def check_login(login):
        #returns bool if login exists
        pass